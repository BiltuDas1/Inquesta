name: Staging Deployment

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

jobs:
  check-changes:
    runs-on: ubuntu-latest
    outputs:
      env_check: ${{ steps.compare.outputs.run_env_check }}
    steps:
      - name: Fetch Remote Hash and Compare
        id: compare
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

          HASH_FILE="/home/${{ secrets.SSH_USER }}/domains/${{ secrets.API_DOMAIN }}/nodejs/.deploy_hash"
          LAST_HASH=$(ssh -o StrictHostKeyChecking=no -p ${{ secrets.SSH_PORT }} -i ~/.ssh/id_rsa ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "cat $HASH_FILE 2>/dev/null || echo 'none'")

          echo "Hostinger currently has hash: $LAST_HASH"
          echo "Current GitHub commit hash: ${{ github.sha }}"

          if [ "$LAST_HASH" == "${{ github.sha }}" ]; then
            echo "No changes detected. Skipping deployment."
            echo "run_env_check=false" >> $GITHUB_OUTPUT
          else
            echo "Fresh deploy needed."
            echo "run_env_check=true" >> $GITHUB_OUTPUT
          fi
        continue-on-error: true

  check-env:
    runs-on: ubuntu-latest
    needs: check-changes
    if: needs.check-changes.outputs.env_check != 'false'
    steps:
      - name: Checking Remote Environment Variables
        id: check_environ
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_KEY }}" > ~/.ssh/id_eddsa
          chmod 600 ~/.ssh/id_eddsa

          ENV_FILE="/home/${{ secrets.SSH_USER }}/domains/${{ secrets.API_DOMAIN }}/public_html/.builds/config/.env"
          ENV_DATA=$(ssh -o StrictHostKeyChecking=no -p ${{ secrets.SSH_PORT }} -i ~/.ssh/id_eddsa ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "cat $ENV_FILE 2>/dev/null || echo 'none'")

          cd ci
          echo $ENV_DATA > .env
          pip install -r requirements.txt
          python checker.py

  deploy-backend-hostinger:
    runs-on: ubuntu-latest
    needs: check-env
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6.0.2

      - name: Setup Node.js
        uses: actions/setup-node@v6.3.0
        with:
          node-version: "24"

      - name: Install and build
        run: |
          cd server
          npm install
          npm run build

          mkdir ../deploy
          cp -r ./dist ../deploy/
          cp -r ./src ../deploy/
          cp ./package.json ../deploy/
          cp ./package-lock.json ../deploy/
          echo "${{ github.sha }}" > ../deploy/.deploy_hash

      - name: Clean old folders on Server
        uses: appleboy/ssh-action@v1.2.5
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            rm -rf /home/${{ secrets.SSH_USER }}/domains/${{ secrets.API_DOMAIN }}/nodejs/dist
            rm -rf /home/${{ secrets.SSH_USER }}/domains/${{ secrets.API_DOMAIN }}/nodejs/src

      - name: Copy files to Hostinger
        uses: appleboy/scp-action@v1.0.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.SSH_PORT }}
          source: "deploy/*"
          target: "/home/${{ secrets.SSH_USER }}/domains/${{ secrets.API_DOMAIN }}/nodejs/"
          strip_components: 1

      - name: Finalize Deployment on Server
        uses: appleboy/ssh-action@v1.2.5
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            cd /home/${{ secrets.SSH_USER }}/domains/${{ secrets.API_DOMAIN }}/nodejs

            export PATH="$PATH:/opt/alt/alt-nodejs24/root/bin"
            npm ci --omit=dev

            mkdir -p tmp
            touch tmp/restart.txt

  deploy-frontend-cloudflare:
    runs-on: ubuntu-latest
    needs: check-env
    steps:
      - name: Checkout Code
        uses: actions/checkout@v6.0.2

      - name: Install Node.js
        uses: actions/setup-node@v6.3.0
        with:
          node-version: "24"

      - name: Build Frontend
        working-directory: ./frontend
        run: |
          npm install
          VITE_API_URL="https://${{ secrets.API_DOMAIN }}" npm run build

      - name: Publish to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3.14.1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy ./frontend/dist --project-name=inquesta-staging --branch main
